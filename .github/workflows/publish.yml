name: Publish to PyPI (disabled — C++ project)

# NOTE: TeleVault is a native C++23 project, not a Python package.
# The legacy PyPI workflow is retained but disabled to avoid failing on
# every `v*` tag. Native multi-arch binaries are built by release.yml.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PyPI publish disabled for C++ project; see release.yml for native binaries."